// Test stub for the `server-only` marker package, which has no standalone
// resolution outside the Next.js bundler. Aliased in vitest.config.mts so test
// files can transitively import modules guarded with `import "server-only"`.
export {}
